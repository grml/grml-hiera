---
classes:
    - apt-keys
    - sudo
    - grml
    - backup
    - mcollective
    - grml::packages

mcollective::middleware_hosts: 
    - father.grml.org

grml::packages::packagelist:
    - psmisc 
